# 📈 Гайд по EMA

EMA (Exponential Moving Average) — проверенная техника для улучшения качества моделей.

---

## 🎯 Что такое EMA?

EMA поддерживает **сглаженную версию весов модели**:

```
EMA_веса = 0.999 × EMA_веса + 0.001 × текущие_веса
```

**Результат:** EMA веса обычно дают лучшее качество на eval, чем финальные веса обучения.

---

## 📊 Почему EMA работает?

| Проблема обычного обучения | Как решает EMA |
|---------------------------|----------------|
| Веса "прыгают" между batch'ами | EMA усредняет изменения |
| Шум в градиентах | Сглаживание убирает шум |
| Последний batch был плохой | EMA помнит всю историю |

**Типичное улучшение:** +1-3% на eval метриках

---

## 🔧 Пример использования

### Базовый пример

```python
from transformers import Trainer, TrainingArguments
from transformers.ema import EMACallback

# 1. Создаём callback
ema_callback = EMACallback(decay=0.999)

# 2. Обычный Trainer
trainer = Trainer(
    model=model,
    args=TrainingArguments(
        output_dir="./output",
        num_train_epochs=3,
        per_device_train_batch_size=4,
    ),
    train_dataset=dataset,
    callbacks=[ema_callback]
)

# 3. Обучаем
trainer.train()

# 4. Применяем EMA веса
ema_callback.apply_ema(model)

# 5. Сохраняем модель с EMA весами
model.save_pretrained("./best_model")
```

---

### Продвинутый пример с EMAModel

```python
from transformers.ema import EMAModel
import torch

# Обёртка для кастомного training loop
ema = EMAModel(model, decay=0.999)

for epoch in range(3):
    for batch in dataloader:
        # Forward
        outputs = model(**batch)
        loss = outputs.loss
        
        # Backward
        loss.backward()
        optimizer.step()
        optimizer.zero_grad()
        
        # Обновляем EMA
        ema.update()
    
    # Eval с EMA весами
    with ema.use_ema():
        eval_results = evaluate(model, eval_dataset)
        print(f"Epoch {epoch}: {eval_results}")

# Финально применяем EMA
ema.apply_ema()
```

---

## 🎛 Выбор decay

| Длина обучения | Decay | Почему |
|----------------|-------|--------|
| ~1,000 шагов | 0.99 | Быстрая адаптация |
| ~10,000 шагов | 0.999 | Стандартный выбор |
| ~100,000 шагов | 0.9999 | Медленная адаптация |

**Правило:** Чем длиннее обучение, тем ближе decay к 1.0

### Автоматический расчёт

```python
from transformers.ema import compute_optimal_decay

decay = compute_optimal_decay(total_steps=10000)
print(f"Recommended: {decay}")  # ~0.9993
```

---

## 💾 Сохранение и загрузка

```python
import torch

# Сохранение EMA state
ema_state = ema_callback.get_ema_state()
torch.save({
    'model': model.state_dict(),
    'ema': ema_state
}, 'checkpoint.pt')

# Загрузка
checkpoint = torch.load('checkpoint.pt')
model.load_state_dict(checkpoint['model'])
ema_callback.load_ema_state(checkpoint['ema'])
ema_callback.apply_ema(model)
```

---

## ⚠️ Важные моменты

### Память

EMA требует **дополнительную память** для хранения копии весов.

```
Дополнительно: ~размер модели в байтах
```

Для 7B модели в fp16: ~14 GB дополнительно.

### Когда применять EMA

1. ✅ **После обучения** — для inference
2. ✅ **На eval** — для отслеживания прогресса
3. ❌ **Во время train forward** — не нужно

### Совместимость

- ✅ Работает с любыми моделями HuggingFace
- ✅ Работает с LoRA/QLoRA
- ✅ Работает с distributed training
- ✅ Работает с gradient checkpointing

---

## 🔬 Когда EMA помогает больше всего

1. **Малые датасеты** — EMA стабилизирует обучение
2. **Высокий learning rate** — EMA сглаживает колебания
3. **Длинное обучение** — накопленный эффект сильнее
4. **Генеративные модели** — особенно заметно в text generation

---

## 📚 Теория

EMA основан на работе Polyak (1990) "Averaging in Stochastic Optimization".

Математически:
```
θ_ema^(t) = β × θ_ema^(t-1) + (1-β) × θ^(t)
```

Используется в:
- Stable Diffusion
- DALL-E 2
- Imagen
- Большинстве SOTA image моделей

---

## 🎯 Резюме

| Усилие | Результат |
|--------|-----------|
| 3 строки кода | +1-3% качества |

```python
from transformers.ema import EMACallback
ema = EMACallback(decay=0.999)
# Добавить в callbacks и вызвать apply_ema() в конце
```
