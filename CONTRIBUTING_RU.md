# Руководство по участию в проекте Transformers Enhanced 🤝

Добро пожаловать! Мы рады каждому участнику проекта и ценим любой вклад.

---

## 📋 Содержание

1. [Способы участия](#способы-участия)
2. [Быстрый старт](#быстрый-старт)
3. [Сообщение о баге](#сообщение-о-баге)
4. [Предложение улучшения](#предложение-улучшения)
5. [Создание Pull Request](#создание-pull-request)
6. [Стиль кода](#стиль-кода)
7. [Запуск тестов](#запуск-тестов)
8. [Синхронизация с upstream](#синхронизация-с-upstream)

---

## 🎯 Способы участия

### Для начинающих

- 🐛 **Сообщить о баге** — нашли проблему? Расскажите нам!
- 📝 **Улучшить документацию** — исправить опечатку, добавить пример
- 💬 **Помочь другим** — ответить на вопросы в issues

### Для опытных

- 🔧 **Исправить баг** — создать Pull Request с фиксом
- ⚡ **Оптимизировать код** — улучшить производительность
- 🆕 **Добавить функционал** — реализовать новую возможность

---

## 🚀 Быстрый старт

### 1. Форкните и клонируйте репозиторий

```bash
# Форкните через GitHub UI, затем:
git clone https://github.com/ВАШ_НИКНЕЙМ/transformers-enhanced.git
cd transformers-enhanced
```

### 2. Добавьте upstream

```bash
git remote add upstream https://github.com/ОСНОВНОЙ_РЕПО/transformers-enhanced.git
git remote add huggingface https://github.com/huggingface/transformers.git
```

### 3. Создайте виртуальное окружение

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
.\venv\Scripts\activate   # Windows
```

### 4. Установите в режиме разработки

```bash
pip install -e ".[dev]"
```

Если возникают ошибки:
```bash
pip install torch
pip install -e ".[quality]"
```

### 5. Создайте ветку для изменений

```bash
git checkout -b fix/краткое-описание-фикса
# или
git checkout -b feature/название-функции
```

> ⚠️ **Никогда** не работайте в ветке `main`!

---

## 🐛 Сообщение о баге

Прежде чем сообщить о баге:

1. ✅ Убедитесь, что баг **ещё не был сообщён** (поиск в Issues)
2. ✅ Проверьте, что это баг в **библиотеке**, а не в вашем коде
3. ✅ Обновите до последней версии и проверьте снова

### Шаблон сообщения о баге

```markdown
## Описание проблемы
Краткое описание бага.

## Воспроизведение
Минимальный код для воспроизведения (до 30 строк):

```python
from transformers import ...

# Код, воспроизводящий проблему
```

## Ожидаемое поведение
Что должно происходить.

## Фактическое поведение
Что происходит на самом деле.

## Окружение
- OS: Windows 11 / Ubuntu 22.04 / macOS 14
- Python: 3.10
- PyTorch: 2.1.0
- Transformers Enhanced: 5.0.0-enhanced.1

## Полный traceback (если есть)
```
Вставьте полный traceback ошибки
```
```

### Получение информации об окружении

```bash
python -c "import transformers; transformers.env"
```

---

## 💡 Предложение улучшения

Если хотите предложить новую функцию:

1. **Мотивация** — зачем это нужно?
2. **Описание** — как это должно работать?
3. **Пример использования** — покажите код
4. **Альтернативы** — рассматривали ли другие решения?

### Приоритетные направления для Enhanced версии

| Направление | Описание |
|-------------|----------|
| 🎯 **LLM Training** | Улучшения для RLHF/DPO/GRPO |
| ⚡ **Производительность** | Оптимизации памяти и скорости |
| 🐛 **Фиксы багов** | Быстрые исправления проблем |
| 📚 **Документация** | Примеры на русском языке |

---

## 📤 Создание Pull Request

### 1. Убедитесь, что ваш код проходит проверки

```bash
# Форматирование и линтинг
make fixup

# Или вручную:
ruff check src/transformers --fix
ruff format src/transformers
```

### 2. Добавьте тесты (если применимо)

```python
# tests/test_ваш_модуль.py
import unittest

class TestВашаФункция(unittest.TestCase):
    def test_основной_случай(self):
        # Ваш тест
        self.assertEqual(expected, actual)
```

### 3. Обновите документацию

- Если добавили новую функцию — опишите её в docstring
- Если исправили баг — добавьте запись в `CHANGELOG.md`

### 4. Создайте коммит

```bash
git add .
git commit -m "fix: краткое описание изменения"
```

#### Формат коммитов

| Префикс | Использование |
|---------|--------------|
| `fix:` | Исправление бага |
| `feat:` | Новая функция |
| `docs:` | Изменения документации |
| `test:` | Добавление тестов |
| `refactor:` | Рефакторинг без изменения функционала |
| `perf:` | Улучшение производительности |

### 5. Отправьте и создайте PR

```bash
git push origin fix/краткое-описание-фикса
```

Затем создайте Pull Request через GitHub UI.

---

## 📐 Стиль кода

### Python

- Используем [Ruff](https://github.com/astral-sh/ruff) для проверки и форматирования
- Следуем PEP 8 с исключениями, определёнными в `pyproject.toml`
- Максимальная длина строки: 119 символов

### Docstrings

```python
def my_function(param1: str, param2: int = 10) -> bool:
    """
    Краткое описание функции.

    Args:
        param1 (`str`):
            Описание первого параметра.
        param2 (`int`, *optional*, defaults to 10):
            Описание второго параметра.

    Returns:
        `bool`: Описание возвращаемого значения.

    Example:
        ```python
        >>> result = my_function("test", 20)
        >>> print(result)
        True
        ```
    """
    return True
```

### Type Hints

```python
from typing import Optional, Union, List

def process_data(
    input_data: Union[str, List[str]],
    max_length: Optional[int] = None,
) -> List[str]:
    ...
```

---

## 🧪 Запуск тестов

### Запуск конкретного теста

```bash
pytest tests/generation/test_configuration_utils.py -v
```

### Запуск тестов для модуля

```bash
pytest tests/models/bert/ -v
```

### Запуск с покрытием

```bash
pytest tests/generation/ --cov=src/transformers/generation --cov-report=html
```

### Пропуск медленных тестов

```bash
pytest tests/ -v --ignore-glob="*slow*"
```

---

## 🔄 Синхронизация с upstream

### С основным Enhanced репозиторием

```bash
git fetch upstream
git checkout main
git merge upstream/main
git push origin main
```

### С официальным Hugging Face

```bash
# Получить обновления
git fetch huggingface

# Посмотреть различия
git log main..huggingface/main --oneline

# Выборочно применить изменения (cherry-pick)
git cherry-pick <commit-hash>

# Или полный merge (с осторожностью!)
git merge huggingface/main
```

---

## 📋 Чеклист перед созданием PR

- [ ] Код проходит `make fixup` без ошибок
- [ ] Добавлены тесты (если применимо)
- [ ] Обновлён `CHANGELOG.md` (для багфиксов и новых функций)
- [ ] Docstrings добавлены/обновлены
- [ ] Коммиты имеют понятные сообщения
- [ ] PR описывает что и зачем было изменено

---

## 🤝 Кодекс поведения

- 💚 Будьте доброжелательны и уважительны
- 💬 Конструктивная критика приветствуется
- 🌍 Помните, что участники из разных стран и культур
- ❌ Не допускается harassment, оскорбления, дискриминация

---

## ❓ Вопросы?

- 📧 Создайте Issue с тегом `question`
- 💬 Обсудите в комментариях к PR

---

**Спасибо за ваш вклад! 🙏**

Каждое исправление, каждое улучшение делает проект лучше для всех.
